pipeline{
  agent any

  environment{
    DOCKERHUB_CREDENTIALS=credentials('dockerid')
    IMAGE_NAME="Darshan/webapp"
  }
  stages
  {
    stage('checkout')
    {
      steps{
        git url:
      }
    }
  }
}
